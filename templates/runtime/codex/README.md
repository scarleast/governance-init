# Codex Runtime Templates

Copy selected canonical skills from `project/governance/skills/**` into `.codex/skills/**` in the target project.

Runtime copies must include a source-of-truth header pointing back to the canonical project-local skill.
